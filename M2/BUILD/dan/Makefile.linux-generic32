# -*- makefile-gmake -*-
DIRECTORY ?= linux32
CONFIGOPTIONS += --enable-strip --enable-tarfile --enable-static --with-issue=Generic --disable-common-staging-area
include Makefile.include
default: arch-chk all check
arch-chk:; [ `uname -m` = i686 ]
# Local Variables:
# compile-command: "time make -f Makefile.linux-generic32 "
# End:
